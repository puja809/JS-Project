// DOM: Document Object Module
// Window is a global object.
let a=window;
// alert("HI");
// prompt("Are u ok?");
a=window.innerHeight;
a=window.innerWidth;
a=scrollX;
a=location;//Gives location of the web page
// a=location.reload();//reloads the page
a=location.href;//Gives the url of the page
a=history.length;//gives the length of the number of web pages we visited in one window
a=history.go(-1);//takes one step back in the web page. Similar to back arrow button in a web page
console.log(a);